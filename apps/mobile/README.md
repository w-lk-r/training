# Mobile App

Expo React Native application with New Architecture enabled.

## Development

```bash
npm run start
```

Then press:
- `i` for iOS simulator
- `a` for Android emulator
- `w` for web browser

Or scan the QR code with Expo Go on your device.
